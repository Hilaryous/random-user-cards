// @flow
import styled from 'react-emotion'

const Header = styled('h1')({
  color: '#294762',
  opacity: '0.8',
  lineHeight: 1.5,
  fontSize: 20,
  textTransform: 'capitalize',
})

export default Header
